{
  "name": "sheephighschoolcomputerscience_website",
  "scripts": {
    "dev": "vitepress dev --port 5176",
    "build": "vitepress build",
    "deploy": "npm run build && wrangler deploy",
    "preview": "vitepress preview && wrangler dev"
  },
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.19",
    "wrangler": "^4.118.0"
  },
  "dependencies": {
    "vue": "^3.5.40"
  },
  "prettier": {
    "bracketSameLine": true,
    "printWidth": 140
  }
}
